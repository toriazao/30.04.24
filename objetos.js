class Calebe {
    constructor (site) {
        this.site = site; //variável de interface
        // ações
    }

    metodo1 (exibirSite) {
        // ações
        window.open(this.www.cifraclub.com.br)
    }

    metodo2 () {
        // ações
    }
}